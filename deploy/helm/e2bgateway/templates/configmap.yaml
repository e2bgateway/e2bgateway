apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "e2bgateway.fullname" . }}
  labels:
    {{- include "e2bgateway.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
